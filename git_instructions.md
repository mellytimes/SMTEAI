1. Open a terminal in `/Users/chanapong/Desktop/my-react-express-app`

2. Initialize the repo:
   ```bash
   git init
   ```

3. Add the remote target:
   ```bash
   git remote add origin https://github.com/mellytimes/SMTEAI.git
   ```

4. (Optional) rename default branch:
   ```bash
   git branch -M main
   ```

5. Stage and commit your files:
   ```bash
   git add .
   git commit -m "Initial commit"
   ```

6. Push to GitHub:
   ```bash
   git push -u origin main
   ```
